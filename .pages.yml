media:
  input: static/assets
  output: /assets
content:
  - name: pages
    label: Pages
    path: src/content/pages
    type: collection
    view:
      fields: [ pubDate, primary, title, draft ]
      primary: title
      sort: [ date, title ]
      default:
        search: ''
        sort: title
        order: desc
